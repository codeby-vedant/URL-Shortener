# URL-Shortener
A simple and efficient URL shortener built with Node.js, Express.js, and MongoDB. It allows users to generate short, shareable links that redirect to original URLs, with support for storing and managing links in a database. 
